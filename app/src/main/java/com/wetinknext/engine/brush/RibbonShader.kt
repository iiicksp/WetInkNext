package com.wetinknext.engine.brush

/** Vertex format consumed by [RibbonRenderer]. Shader sources live in ShaderLib. */
object RibbonShader { const val FLOATS_PER_VERTEX = 4; const val VERTEX_STRIDE_BYTES = FLOATS_PER_VERTEX * Float.SIZE_BYTES }
